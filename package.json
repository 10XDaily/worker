{
    "name": "worker",
    "private": true,
    "version": "1.0.0",
    "description": "Routing worker.10X.day links, Worker CRON, Worker Pages.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Chris Jacob <chris@10x.day>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.18.2"
    },
    "dependencies": {
        "itty-router": "^2.3.10",
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
